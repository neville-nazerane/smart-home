@page "/"

@if (devices is not null)
{
    @foreach (var deviceGrp in devices.Chunk(3))
    {
        <div class="row">
            @foreach (var device in deviceGrp)
            {
            <div class="col">
                <Device DeviceModel=device />
            </div>
            }
        </div>
        @*<div><i>@device.Id</i>  <b>@device.Name</b> </div>*@

    }
}